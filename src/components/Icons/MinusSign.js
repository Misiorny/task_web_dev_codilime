import minusSign from '../../img/png/minus.png';

function MinusSign(props) {
	return (
		<img className="minusSign" src={minusSign} alt={props.alt} width={44} height={44}/>
	)
}

export default MinusSign;
